substitutions:
  logger_level: INFO
  public: true

logger:
  level: ${logger_level}

api:

ota:
  platform: esphome

time:
  - platform: sntp
    id: sntp_

wifi:
  ap:

web_server:
  port: 80

captive_portal:
